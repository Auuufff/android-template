buildscript {

    ext {

        // All project module configs should be in config map
        config = [
                applicationId    : "com.example.projectbase",
                minSdkVersion    : 23,
                targetSdkVersion : 29,
                buildToolsVersion: "29.0.3",
                versionMajor     : "1.0",
                buildNumber      : (project.hasProperty('buildNumber') ? Integer.parseInt(buildNumber) : 1),
                releaseNotes     : (project.hasProperty('releaseNotes') ? releaseNotes : "")
        ]

        // All project modules dependency versions should be in versions map
        versions = [
                kotlin            : '1.3.72',
                coroutines        : '1.3.5',
                appcompat         : '1.1.0',
                androidx_core     : '1.2.0',
                fragment_ktx      : '1.2.4',
                dagger            : '2.27',
                lifecycle_version : '2.2.0',
                constraint_layout : '2.0.0-beta6',
                timber            : '4.7.1',
                nav_version       : '2.2.2',
                material          : '1.2.0-alpha06',
                recycler_view     : '1.1.0',
                corbind           : '1.3.1',
                live_event        : '1.2.0',
                fragment_ktx      : '1.2.3',
                kpermissions      : '3.1.0',
        ]
    }
}
